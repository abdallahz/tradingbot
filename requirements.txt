PyYAML>=6.0
alpaca-py>=0.43.0
requests>=2.32.0
pandas>=2.0.0
feedparser>=6.0.10
lxml>=4.9.0
pytz>=2025.1
ta>=0.11.0              # Free technical indicators (RSI, MACD, ATR, Bollinger Bands)

# Optional: AI sentiment analysis (Phase 6) — free local model
# transformers>=4.38.0  # For FinBERT (free, no API key)
# torch>=2.0.0          # Required by FinBERT

# Optional: Paid AI sentiment providers (future use)
# openai>=1.0.0         # For OpenAI GPT-4 sentiment
# anthropic>=0.18.0     # For Anthropic Claude sentiment

# Development dependencies
pytest>=7.0
