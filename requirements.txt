PyYAML>=6.0
alpaca-py>=0.43.0
requests>=2.32.0
pandas>=2.0.0
feedparser>=6.0.10
lxml>=4.9.0
pytz>=2025.1
python-dotenv>=1.0.0   # Load .env file for local dev
ta>=0.11.0              # Free technical indicators (RSI, MACD, ATR, Bollinger Bands)
mplfinance==0.12.10b0    # Candlestick chart generation (Phase 7)
matplotlib>=3.7.0       # Required by mplfinance

# Optional: AI sentiment analysis (Phase 6) — free local model (FinBERT)
# torch and transformers are NOT installed on Heroku (slug would exceed 1GB limit).
# The sentiment analyzer degrades gracefully when these are absent.
# To enable locally: pip install transformers torch
# transformers>=4.38.0
# torch>=2.0.0

# Optional: Paid AI sentiment providers (future use)
# openai>=1.0.0         # For OpenAI GPT-4 sentiment
# anthropic>=0.18.0     # For Anthropic Claude sentiment

# Development dependencies
pytest>=7.0
