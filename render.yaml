# All cron jobs migrated to VPS (178.156.202.27) as of 2026-04-15.
# Dashboard served by gunicorn + nginx on VPS.
# Render crons disabled — VPS crontab is the sole scheduler.
# Backup of original config preserved in branch: main-render
#
# To restore: git checkout main-render -- render.yaml